# ============================================================
# BootZone Client - Environment Variables
# File: client/.env
# ============================================================
# Left unset intentionally: api.js already falls back to
# http://localhost:5000/api in dev and /api in production builds.
